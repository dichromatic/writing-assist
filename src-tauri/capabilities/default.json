{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability set for local development.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
